# คู่มืออธิบายการทำงานของระบบ e-Utilities (ฉบับเข้าใจง่ายสำหรับทุกคน)

ระบบ **e-Utilities** คือ **ระบบบันทึก ควบคุม และติดตามค่าใช้จ่ายสาธารณูปโภค** (เช่น ค่าน้ำ, ค่าไฟฟ้า, ค่าอินเทอร์เน็ต, ค่าโทรศัพท์, ค่าเช่าอาคาร) เพื่อช่วยให้หน่วยงานหรือองค์กรสามารถเช็กได้ว่า **ในแต่ละเดือนตั้งงบประมาณไว้เท่าไหร่ ใช้เงินไปแล้วเท่าไหร่ เกินงบหรือไม่ พร้อมทั้งพิมพ์รายงานออกมาได้ทันที**

---

## 🏢 เปรียบเทียบภาพรวมของระบบเหมือน "สำนักงาน / ร้านอาหาร"

เพื่อให้เข้าใจง่ายที่สุด ให้จินตนาการว่าระบบนี้เหมือนกับการทำงานในร้านค้าที่มี 4 ฝ่าย:

```
[ผู้ใช้งาน (User)] 
       │
       ▼
 1. 🖥️ หน้าร้าน (Frontend - React)  ─── กดปุ่ม/ดูข้อมูล/กรอกแบบฟอร์ม
       │ (ส่งใบสั่ง/ส่งคำขอ)
       ▼
 2. 👨‍🍳 ครัวหลังร้าน (Backend - Node.js) ─── ตรวจสอบสิทธิ์ คำนวณยอดเงิน สร้างรายงาน PDF
       │ (หยิบ/บันทึกวัตถุดิบ)
       ▼
 3. 📦 ห้องเก็บของ (Database - MariaDB) ─── ตู้เซฟเก็บข้อมูลตัวเลข บัญชีผู้ใช้ ประวัติรายการ
       │
 4. 🗄️ สมุดตรวจนับสต็อก (phpMyAdmin) ─── หน้าจอสำหรับผู้ดูแลระบบเข้าไปดูหลังบ้าน
```

---

## 1. กล่องควบคุมระบบใหญ่ (`docker-compose.yml`)

### 📦 Docker คืออะไร?
เปรียบเหมือน **"กล่องพัสดุสำเร็จรูป"** ที่รวบรวมโปรแกรม อุปกรณ์ และการตั้งค่าทุกอย่างไว้ข้างใน ทำให้เราสามารถยกกล่องนี้ไปเปิดรันบนคอมพิวเตอร์เครื่องไหนในโลกก็ได้ โดยไม่ต้องมานั่งลงโปรแกรมเสริมทีละตัวให้ยุ่งยาก

### ⚙️ ในโปรเจกต์นี้มีกล่องอะไรทำงานร่วมกันบ้าง?
1. **`db` (MariaDB):** เครื่องแม่ข่ายฐานข้อมูล ทำหน้าที่เก็บข้อมูลทุกอย่างลงดิสก์อย่างถาวร
2. **`phpmyadmin`:** เว็บเครื่องมือช่วยให้เรากดดูตารางและแก้ไขฐานข้อมูลได้ง่ายๆ ผ่านเบราว์เซอร์ที่พอร์ต 8080
3. **`backend`:** หัวใจหลักของการประมวลผล (API) คอยคุยกับฐานข้อมูลและส่งข้อมูลให้หน้าเว็บ ที่พอร์ต 5000
4. **`frontend`:** หน้าจอเว็บสวยงามที่ผู้ใช้งานทั่วไปมองเห็นและกดใช้งาน ที่พอร์ต 5173

---

## 2. ส่วนหน้าบ้านที่ผู้ใช้งานมองเห็น (`frontend/`)

เปรียบเหมือน **"เคาน์เตอร์บริการหน้าร้าน"** ที่มีปุ่ม เมนู ฟอร์มกรอก และกราฟแสดงผลสวยงาม

### 📄 หน้าจอแต่ละหน้าทำหน้าที่อะไร?
* **หน้าเข้าสู่ระบบ / สมัครสมาชิก (`Login.jsx`, `Register.jsx`):** 
  * สำหรับกรอกชื่อผู้ใช้และรหัสผ่านเพื่อเข้าใช้งาน
* **หน้าแดชบอร์ดภาพรวม (`Dashboard.jsx`):**
  * สรุปตัวเลขสำคัญให้ผู้บริหารดู เช่น เดือนนี้ใช้เงินไปกี่บาท, เหลืองบประมาณเท่าไหร่ (คิดเป็นกี่ %), มีกราฟแท่งเปรียบเทียบค่าใช้จ่ายแต่ละประเภท
* **หน้ารายการค่าใช้จ่าย (`Transactions.jsx`):**
  * สมุดบันทึกรายจ่ายจริง เช่น วันที่ 5 จ่ายค่าไฟไป 8,500 บาท สามารถกดเพิ่ม แก้ไข หรือลบรายการได้
* **หน้าหมวดงบประมาณ (`BudgetCategories.jsx`):**
  * สำหรับตั้งเป้าหมายงบประมาณ เช่น ตั้งงบสาธารณูปโภคประจำเดือนสิงหาคมไว้ 50,000 บาท
* **หน้าประเภทค่าใช้จ่าย (`ExpenseTypes.jsx`):**
  * สำหรับจัดการหมวดหมู่รายจ่าย เช่น เพิ่มหมวด "ค่าซ่อมบำรุง", "ค่าน้ำประปา", "ค่าไฟฟ้า"
* **หน้ารายงาน (`Reports.jsx`):**
  * สรุปรายงานรายเดือน พร้อมปุ่มดาวน์โหลดเป็น **ไฟล์ PDF** หรือ **Excel** เพื่อนำไปเบิกจ่ายเงิน

### 🧩 ชิ้นส่วนประกอบเสริม (Components & Context):
* **`Navbar.jsx` & `Layout.jsx`:** แถบเมนูด้านซ้ายและด้านบน ช่วยให้กดสลับหน้าไปมาได้ง่าย
* **`BarChart.jsx`:** ตัววาดกราฟแท่ง แสดงผลเปรียบเทียบตัวเลขให้ดูเข้าใจง่าย
* **`ExportButtons.jsx`:** ปุ่มกดเพื่อดาวน์โหลดไฟล์เอกสาร PDF/Excel
* **`AuthContext.jsx` (บัตรพนักงานดิจิทัล):** จำไว้ว่าใครกำลังล็อกอินอยู่ ถ้าปิดหน้าต่างแล้วเปิดใหม่ก็ไม่ต้องล็อกอินซ้ำ

---

## 3. ส่วนหลังบ้านและการประมวลผล (`backend/`)

เปรียบเหมือน **"ผู้จัดการและเจ้าหน้าที่บัญชีหลังร้าน"** คอยรับคำสั่ง ตรวจสอบความถูกต้อง และคำนวณตัวเลข

### 📂 ไฟล์สำคัญและหน้าที่:
* **`server.js` (ประตูใหญ่):**
  * ตัวเปิดระบบหลังบ้าน คอยรอรับคำขอจากหน้าเว็บที่ส่งเข้ามา
* **`config/autoInitDB.js` (ผู้ช่วยเปิดร้านอัตโนมัติ):**
  * ทันทีที่เปิดระบบ ตัวนี้จะไปตรวจตู้เซฟฐานข้อมูลทันที ถ้าพบว่าเป็นระบบใหม่ที่ยังไม่มีข้อมูล มันจะสร้างตารางและเติมข้อมูลตัวอย่าง (ค่าน้ำ, ค่าไฟ, งบประมาณ, ผู้ใช้ `admin`) ให้ทันทีโดยอัตโนมัติ
* **`middleware/authMiddleware.js` (รปภ. ตรวจบัตร):**
  * คอยตรวจเช็ก Token (บัตรผ่าน) ก่อนจะอนุญาตให้ดึงข้อมูลหรือบันทึกข้อมูล ป้องกันไม่ให้คนนอกที่ไม่ได้ล็อกอินเข้ามาดูข้อมูลได้

### 🧠 แผนกคำนวณและประมวลผล (`controllers/`):
* **`authController.js`:** ตรวจสอบความถูกต้องของรหัสผ่านตอนล็อกอิน และเข้ารหัสความปลอดภัย
* **`dashboardController.js`:** คำนวณตัวเลขสรุป เช่น ยอดรวมรายจ่ายเดือนนี้, หักลบกับงบประมาณ, จัดกลุ่มข้อมูลส่งไปทำกราฟ
* **`transactionController.js`:** บันทึกรายจ่ายจริงลงสมุดบัญชี
* **`budgetCategoryController.js`:** จัดการยอดเงินงบประมาณที่ตั้งไว้
* **`exportController.js` (โรงพิมพ์เอกสาร):** ดึงข้อมูลยอดเงินทั้งหมดมาจัดหน้าและพิมพ์ออกมาเป็น **ไฟล์ PDF** หรือส่งออกเป็น **ตาราง Excel (.xlsx)**

---

## 4. ตู้เซฟเก็บข้อมูล (`Database Schema`)

เปรียบเหมือน **"แฟ้มเอกสาร 4 เล่ม"** ที่บันทึกข้อมูลเชื่อมโยงกันอย่างเป็นระเบียบ:

```
[ 1. users ] ────────── ใครเป็นคนบันทึกรายการ
      │
      ▼
[ 4. transactions ] ─── รายการจ่ายเงินจริง (กี่บาท / วันไหน)
      │          │
      ▼          ▼
[ 2. expense_types ]  [ 3. budget_categories ]
 (จ่ายเป็นค่าอะไร เช่น ค่าไฟ)   (หักจากงบก้อนไหน เช่น งบประจำเดือน ส.ค.)
```

1. **`users` (แฟ้มรายชื่อผู้ใช้งาน):**
   * เก็บชื่อผู้ใช้ (`username`) และรหัสผ่านที่ถูกเข้ารหัสความปลอดภัย (`password_hash`)
2. **`expense_types` (แฟ้มประเภทค่าใช้จ่าย):**
   * เก็บชื่อประเภท เช่น "ค่าไฟฟ้า", "ค่าน้ำประปา", "ค่าอินเทอร์เน็ต"
3. **`budget_categories` (แฟ้มวงเงินงบประมาณ):**
   * เก็บวงเงินงบประมาณที่ตั้งไว้ เช่น "งบสาธารณูปโภค ประจำเดือน 8/2026 จำนวน 50,000 บาท"
4. **`transactions` (แฟ้มบันทึกรายจ่ายจริง):**
   * บันทึกว่าจ่ายเงินกี่บาท, จ่ายวันที่เท่าไหร่, เป็นค่าอะไร (ดึงจากแฟ้มที่ 2), หักจากงบก้อนไหน (ดึงจากแฟ้มที่ 3), และใครเป็นคนลงบันทึก (ดึงจากแฟ้มที่ 1)

---

## 💡 สรุปใน 3 บรรทัด
1. **Frontend (หน้าร้าน):** ให้คนใช้กรอกข้อมูล ดูตัวเลข และดูกราฟ
2. **Backend (หลังร้าน):** ตรวจสอบสิทธิ์ คำนวณเงิน สร้างรายงาน PDF/Excel และสั่งการฐานข้อมูล
3. **Database & Docker (ตู้เซฟและกล่องบรรจุ):** เก็บข้อมูลทั้งหมดอย่างปลอดภัย และช่วยให้ยกทั้งระบบไปเปิดที่ไหนก็รันได้ทันที
